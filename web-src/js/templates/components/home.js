import React from 'react';

const template = (
    <form>
        <fieldset>
            <input type="text" name="hashtag" placeholder="Enter a hashtag here!" required="true" />
            <button>Go</button>
        </fieldset>
    </form>
);
module.exports = template;